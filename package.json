{
  "name": "pizza-delivery-application",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run build --prefix client && concurrently \"npm run dev --prefix server\" \"npm run preview --prefix client -- --host 0.0.0.0\"",
    "client:dev": "npm run dev --prefix client",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
